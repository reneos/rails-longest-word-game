<p>Enter the longest valid word you can think of into the box and click submit! You can only use each letter once.</p>
<p><strong>Points earned so far: </strong><%= @total_score %></p>
<p><strong>Games played so far: </strong><%= @played %></p>
  <form action="/new">
    <button type="submit" name="reset" value="true">Reset metrics?</button>
  </form>
<ul>
  <% @letters.each do |letter| %>
    <li><%= letter %></li>
  <% end %>
</ul>

<form action="/score" method="POST">
  <p>What is the longest word you can find?</p>
  <%= hidden_field_tag 'letters', @letters %>
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <input type="text" name="answer">
  <button type="submit">Play</button>
</form>
